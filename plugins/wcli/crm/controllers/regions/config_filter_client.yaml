# ===================================
# Filter Scope Definitions
# ===================================

scopes:
    segmentation:
        label: Segmentation
        scope: tagFilter
        modelClass: Waka\Segator\Models\Tag
        nameFrom: name

    num_id:
        label: ID client
        type: number
        conditions: id = ':filtered'
        size: 1